# Adapter Boundary

This package exists to reserve the adapter boundary required by the MVP architecture.

E0-T01 intentionally does **not** implement any active agent adapter. Adapter implementations are introduced only in E3 for the approved CLI agents.
